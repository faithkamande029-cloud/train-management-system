# Deployed API (include /api because the Render backend prefixes its routes)
# VITE_API_URL=https://train-management-backend.onrender.com/api

# For the included mock API, use this value (or omit VITE_API_URL entirely):
# VITE_API_URL=/api
# VITE_AUTH_MODE=demo

# To call the mock server directly instead, use:
# VITE_API_URL=http://localhost:3001
